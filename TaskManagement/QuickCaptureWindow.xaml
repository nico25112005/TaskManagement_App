<Window x:Class="TaskManagement.QuickCaptureWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Quick capture" Height="120" Width="440" WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize" ShowInTaskbar="False"
        Background="#ededed" Topmost="True">

    <Border Background="#ffffff" Margin="12" BorderBrush="#abadb3" BorderThickness="1" CornerRadius="4" Padding="12">
        <StackPanel>
            <TextBlock Text="New task" Foreground="Gray" FontSize="11" Margin="0,0,0,4"/>
            <DockPanel LastChildFill="True">
                <TextBox x:Name="Tb_input" Padding="6,4" FontSize="14"
                         KeyDown="Tb_input_KeyDown"
                         Background="Transparent" BorderThickness="0"/>
            </DockPanel>
            <DockPanel Margin="0,6,0,0" LastChildFill="False">
                <TextBlock DockPanel.Dock="Left" Text="Enter to save \u2014 Esc to cancel" FontSize="10" Foreground="Gray" VerticalAlignment="Center"/>
            </DockPanel>
        </StackPanel>
    </Border>
</Window>
